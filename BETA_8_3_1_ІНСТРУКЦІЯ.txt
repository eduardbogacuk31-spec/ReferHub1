REFERHUB BETA 8.3.1 — SOCIAL HUB FIXED

ВИПРАВЛЕНО:
- вкладка «Друзі» знову відкривається;
- leaderboard більше не звертається до неіснуючого поля level;
- рівень обчислюється з реального поля xp;
- nullable last_seen більше не ламає /api/friends;
- помилка рейтингу не блокує список друзів;
- порожній список друзів відображається коректно;
- milestones захищені від відсутніх даних;
- внутрішні вкладки працюють окремо;
- додано кнопку повторного завантаження при помилці.

ДОДАНО:
- рейтинг;
- топ-3;
- ліги;
- перегляд профілю;
- реферальні рівні;
- мобільна адаптація.

БАЗА:
Beta 8.2 Progression Center.

ЗАПУСК:
py -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
$env:PORT=8081
.\.venv\Scripts\python.exe .\app.py

САЙТ:
http://127.0.0.1:8081

ПІСЛЯ ОНОВЛЕННЯ:
Ctrl + Shift + R
